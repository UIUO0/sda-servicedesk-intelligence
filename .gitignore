# Secrets / config
.env

# Data (raw pulls and processed outputs — contain real PII: names, emails, phones)
data/

# Raw API response dumps anywhere in the tree (PII). Keep .env.example etc. tracked.
apiResponse*.json
*_raw.json

# Generated insight artefacts (may embed PII in labels)
insights/*.png
insights/summary.md
insights/eda_summary.md

# Trained model artifacts (TF-IDF vocabularies embed ticket-text fragments)
models/artifacts/

# Python
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
