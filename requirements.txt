requests>=2.31
python-dotenv>=1.0
pandas>=2.0
pyarrow>=14.0
beautifulsoup4>=4.12
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.66
streamlit>=1.30
plotly>=5.18
scikit-learn>=1.4

# ML apps: routing (xgboost optional), clustering (embeddings + hdbscan/umap),
# forecasting (prophet). truststore lets HuggingFace downloads trust the
# corporate TLS-inspection proxy via the Windows cert store.
xgboost>=2.0
hdbscan>=0.8.33
umap-learn>=0.5.5
sentence-transformers>=3.0
truststore>=0.9
prophet>=1.1
